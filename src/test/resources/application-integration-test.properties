server.error.include-message=always

minio.bucket.name=tmp-bucket
minio.access.key=${embedded.minio.accessKey}
minio.access.secret=${embedded.minio.secretKey}
minio.url=http://${embedded.minio.host}:${embedded.minio.port}